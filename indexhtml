<!DOCTYPE html>
<html lang="pt-BR">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Diagnóstico de Automação — Martz</title>
<link href="https://fonts.googleapis.com/css2?family=DM+Sans:wght@300;400;500;600;700&family=DM+Mono:wght@400;500&display=swap" rel="stylesheet">
<style>
:root {
  --martz: #7B5EA7;
  --martz-light: #F0EBF8;
  --martz-mid: #C4B0E0;
  --martz-dark: #5A3F85;
  --ch-orange: #FF8C42;
  --ch-orange-light: #FFF5EE;
  --green: #10B981;
  --green-light: #ECFDF5;
  --amber: #F59E0B;
  --amber-light: #FFFBEB;
  --red: #EF4444;
  --dark: #0F0F14;
  --dark2: #1A1A2E;
  --gray: #6B7280;
  --gray-light: #F8F8FB;
  --gray-border: #E8E6F0;
  --white: #FFFFFF;
  --text: #1A1A2E;
}

* { margin: 0; padding: 0; box-sizing: border-box; }

body {
  font-family: 'DM Sans', sans-serif;
  background: var(--gray-light);
  color: var(--text);
  min-height: 100vh;
}

/* ── HEADER ── */
.site-header {
  background: var(--white);
  border-bottom: 1px solid var(--gray-border);
  padding: 0 2rem;
  display: flex;
  align-items: center;
  justify-content: space-between;
  height: 60px;
  position: sticky;
  top: 0;
  z-index: 100;
}

.logos { display: flex; align-items: center; gap: 1.5rem; }

.logo-martz {
  font-family: 'DM Sans', sans-serif;
  font-size: 1.5rem;
  font-weight: 700;
  color: var(--martz);
  letter-spacing: -0.03em;
}

.logo-sep {
  width: 1px; height: 24px;
  background: var(--gray-border);
}

.logo-ch {
  display: flex; align-items: center; gap: 0.4rem;
  font-size: 0.85rem; font-weight: 500; color: var(--gray);
}

.logo-ch-dot {
  width: 18px; height: 18px;
  background: var(--ch-orange);
  border-radius: 50%;
}

.header-badge {
  background: var(--martz-light);
  color: var(--martz);
  font-size: 0.75rem;
  font-weight: 600;
  padding: 0.3rem 0.75rem;
  border-radius: 20px;
  letter-spacing: 0.02em;
}

/* ── NAV TABS ── */
.nav-tabs {
  background: var(--white);
  border-bottom: 1px solid var(--gray-border);
  display: flex;
  padding: 0 2rem;
  gap: 0;
  overflow-x: auto;
}

.nav-tab {
  padding: 0.875rem 1.25rem;
  font-size: 0.85rem;
  font-weight: 500;
  color: var(--gray);
  cursor: pointer;
  border-bottom: 2px solid transparent;
  white-space: nowrap;
  transition: all 0.2s;
  background: none;
  border-top: none;
  border-left: none;
  border-right: none;
  font-family: 'DM Sans', sans-serif;
}
.nav-tab:hover { color: var(--martz); }
.nav-tab.active {
  color: var(--martz);
  border-bottom-color: var(--martz);
  font-weight: 600;
}

/* ── MAIN ── */
.main { max-width: 1100px; margin: 0 auto; padding: 2.5rem 2rem; }

.tab-content { display: none; }
.tab-content.active { display: block; }

/* ── SECTION TITLES ── */
.section-header {
  margin-bottom: 2rem;
}
.section-title {
  font-size: 1.5rem;
  font-weight: 700;
  color: var(--text);
  letter-spacing: -0.02em;
}
.section-subtitle {
  font-size: 0.875rem;
  color: var(--gray);
  margin-top: 0.35rem;
}
.section-rule {
  width: 32px; height: 3px;
  background: var(--martz);
  border-radius: 2px;
  margin-top: 0.6rem;
}

/* ── HERO CARDS ── */
.hero-grid {
  display: grid;
  grid-template-columns: 1fr 1fr 1fr 1fr;
  gap: 1rem;
  margin-bottom: 2rem;
}

.hero-card {
  background: var(--white);
  border: 1px solid var(--gray-border);
  border-radius: 14px;
  padding: 1.5rem;
  position: relative;
  overflow: hidden;
}

.hero-card.featured {
  background: linear-gradient(135deg, var(--martz-dark) 0%, var(--martz) 100%);
  border-color: transparent;
  grid-column: span 1;
}

.hero-card .label {
  font-size: 0.75rem;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.06em;
  color: var(--gray);
  margin-bottom: 0.5rem;
}
.hero-card.featured .label { color: rgba(255,255,255,0.7); }

.hero-card .value {
  font-size: 2rem;
  font-weight: 700;
  color: var(--text);
  letter-spacing: -0.03em;
  line-height: 1;
}
.hero-card.featured .value { color: white; font-size: 2.4rem; }

.hero-card .sub {
  font-size: 0.8rem;
  color: var(--gray);
  margin-top: 0.4rem;
}
.hero-card.featured .sub { color: rgba(255,255,255,0.75); }

.hero-card .accent-bar {
  position: absolute;
  bottom: 0; left: 0;
  height: 3px; width: 100%;
  background: var(--martz);
  border-radius: 0 0 14px 14px;
}
.hero-card.featured .accent-bar { background: rgba(255,255,255,0.3); }

/* ── TAG CARDS ── */
.tag-grid {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 1rem;
  margin-bottom: 2rem;
}

.tag-card {
  background: var(--white);
  border: 1px solid var(--gray-border);
  border-radius: 12px;
  padding: 1.25rem;
  border-top: 3px solid var(--martz);
}
.tag-card.green { border-top-color: var(--green); }
.tag-card.orange { border-top-color: var(--ch-orange); }
.tag-card.red { border-top-color: var(--red); }
.tag-card.blue { border-top-color: #3B82F6; }

.tag-name {
  font-weight: 700;
  font-size: 0.95rem;
  color: var(--text);
  margin-bottom: 0.25rem;
}
.tag-badge {
  display: inline-block;
  font-size: 0.7rem;
  font-weight: 700;
  padding: 0.2rem 0.5rem;
  border-radius: 6px;
  margin-bottom: 0.75rem;
  background: var(--martz-light);
  color: var(--martz);
}
.tag-card.green .tag-badge { background: var(--green-light); color: var(--green); }
.tag-card.orange .tag-badge { background: var(--ch-orange-light); color: var(--ch-orange); }
.tag-card.red .tag-badge { background: #FEF2F2; color: var(--red); }
.tag-card.blue .tag-badge { background: #EFF6FF; color: #3B82F6; }

.tag-desc {
  font-size: 0.8rem;
  color: var(--gray);
  line-height: 1.5;
}

/* ── COMPOSITION BARS ── */
.comp-section {
  background: var(--white);
  border: 1px solid var(--gray-border);
  border-radius: 14px;
  padding: 1.75rem;
}
.comp-title {
  font-size: 1rem;
  font-weight: 700;
  margin-bottom: 0.25rem;
}
.comp-sub {
  font-size: 0.8rem;
  color: var(--gray);
  margin-bottom: 1.5rem;
}
.bar-row {
  display: flex;
  align-items: center;
  gap: 1rem;
  margin-bottom: 0.875rem;
}
.bar-label {
  font-size: 0.8rem;
  font-weight: 500;
  color: var(--text);
  width: 280px;
  flex-shrink: 0;
}
.bar-track {
  flex: 1;
  height: 28px;
  background: var(--gray-light);
  border-radius: 6px;
  overflow: hidden;
}
.bar-fill {
  height: 100%;
  border-radius: 6px;
  display: flex;
  align-items: center;
  padding-left: 0.75rem;
  transition: width 0.8s ease;
}
.bar-fill span {
  font-size: 0.72rem;
  font-weight: 600;
  color: white;
  white-space: nowrap;
}
.bar-pct {
  font-size: 0.85rem;
  font-weight: 700;
  color: var(--text);
  width: 42px;
  text-align: right;
}
.bar-tag {
  font-size: 0.7rem;
  font-weight: 600;
  padding: 0.15rem 0.5rem;
  border-radius: 4px;
  width: 70px;
  text-align: center;
}
.bar-tag.conteudo { background: var(--green-light); color: var(--green); }
.bar-tag.edicao   { background: var(--ch-orange-light); color: var(--ch-orange); }

/* ── CALCULADORA ── */
.calc-wrapper {
  background: var(--white);
  border: 1.5px solid var(--martz-mid);
  border-radius: 18px;
  overflow: hidden;
  margin-bottom: 2rem;
}

.calc-header {
  background: linear-gradient(135deg, var(--martz-dark) 0%, var(--martz) 100%);
  padding: 1.5rem 2rem;
  color: white;
}
.calc-header h3 {
  font-size: 1.1rem;
  font-weight: 700;
  margin-bottom: 0.25rem;
}
.calc-header p {
  font-size: 0.82rem;
  opacity: 0.8;
}

.calc-body { padding: 2rem; }

/* Input de agentes */
.agents-input-row {
  display: flex;
  align-items: center;
  gap: 1.5rem;
  background: var(--martz-light);
  border-radius: 12px;
  padding: 1.25rem 1.5rem;
  margin-bottom: 1.75rem;
}
.agents-label {
  font-size: 0.875rem;
  font-weight: 600;
  color: var(--martz-dark);
  flex: 1;
}
.agents-label span {
  display: block;
  font-size: 0.75rem;
  font-weight: 400;
  color: var(--martz);
  margin-top: 0.2rem;
}
.agents-control {
  display: flex;
  align-items: center;
  gap: 0.75rem;
}
.agent-btn {
  width: 36px; height: 36px;
  border-radius: 50%;
  border: 2px solid var(--martz);
  background: white;
  color: var(--martz);
  font-size: 1.2rem;
  font-weight: 700;
  cursor: pointer;
  display: flex; align-items: center; justify-content: center;
  transition: all 0.15s;
  font-family: 'DM Sans', sans-serif;
}
.agent-btn:hover { background: var(--martz); color: white; }

#agentCount {
  font-size: 2rem;
  font-weight: 700;
  color: var(--martz);
  min-width: 2.5rem;
  text-align: center;
  font-family: 'DM Mono', monospace;
}

.agents-meta {
  text-align: right;
  font-size: 0.8rem;
  color: var(--gray);
}
.agents-meta strong {
  display: block;
  font-size: 1rem;
  font-weight: 700;
  color: var(--text);
}

/* Premissas fixas */
.premissas-row {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 0.75rem;
  margin-bottom: 1.75rem;
}
.prem-card {
  background: var(--gray-light);
  border-radius: 10px;
  padding: 0.875rem 1rem;
  text-align: center;
}
.prem-val {
  font-size: 1.05rem;
  font-weight: 700;
  color: var(--martz);
  font-family: 'DM Mono', monospace;
}
.prem-label {
  font-size: 0.72rem;
  color: var(--gray);
  margin-top: 0.2rem;
  line-height: 1.3;
}

/* Cenários */
.scenarios-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 1rem;
}

.scenario-card {
  border-radius: 14px;
  border: 1.5px solid var(--gray-border);
  overflow: hidden;
  transition: transform 0.2s, box-shadow 0.2s;
}
.scenario-card:hover {
  transform: translateY(-2px);
  box-shadow: 0 8px 24px rgba(0,0,0,0.08);
}
.scenario-card.featured {
  border-color: var(--martz);
  box-shadow: 0 4px 20px rgba(123,94,167,0.15);
}

.sc-head {
  padding: 1rem 1.25rem;
  font-weight: 700;
  font-size: 0.9rem;
  color: var(--gray);
  background: var(--gray-light);
  display: flex;
  justify-content: space-between;
  align-items: center;
}
.scenario-card.featured .sc-head {
  background: var(--martz);
  color: white;
}
.sc-star { font-size: 0.8rem; opacity: 0.8; }

.sc-body { padding: 1.25rem; background: white; }

.sc-metric {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 0.5rem 0;
  border-bottom: 1px solid var(--gray-border);
  font-size: 0.82rem;
}
.sc-metric:last-child { border-bottom: none; }
.sc-metric-label { color: var(--gray); }
.sc-metric-val {
  font-weight: 700;
  color: var(--text);
  font-family: 'DM Mono', monospace;
  font-size: 0.85rem;
}
.sc-metric-val.green { color: var(--green); }
.sc-metric-val.martz { color: var(--martz); }

.sc-eco {
  margin-top: 0.875rem;
  padding: 0.875rem;
  background: var(--green-light);
  border-radius: 10px;
  text-align: center;
}
.sc-eco-label {
  font-size: 0.72rem;
  color: var(--gray);
  font-weight: 500;
  text-transform: uppercase;
  letter-spacing: 0.05em;
}
.sc-eco-val {
  font-size: 1.4rem;
  font-weight: 700;
  color: var(--green);
  font-family: 'DM Mono', monospace;
  margin-top: 0.2rem;
}
.sc-eco-year {
  font-size: 0.75rem;
  color: var(--gray);
  margin-top: 0.2rem;
}

/* Sem/Com CloudChat toggle */
.cc-toggle-row {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 0.5rem;
  margin-bottom: 1.25rem;
}
.cc-btn {
  padding: 0.45rem 1rem;
  border-radius: 20px;
  font-size: 0.8rem;
  font-weight: 600;
  cursor: pointer;
  border: 1.5px solid var(--gray-border);
  background: white;
  color: var(--gray);
  transition: all 0.15s;
  font-family: 'DM Sans', sans-serif;
}
.cc-btn.active {
  background: var(--martz);
  border-color: var(--martz);
  color: white;
}

/* ── AGENTS VISUAL ── */
.agents-visual {
  display: flex;
  gap: 0.5rem;
  justify-content: center;
  margin: 0.75rem 0;
  flex-wrap: wrap;
}
.agent-dot {
  width: 28px; height: 36px;
  position: relative;
}
.agent-dot::before {
  content: '';
  position: absolute;
  top: 0; left: 50%; transform: translateX(-50%);
  width: 12px; height: 12px;
  border-radius: 50%;
  background: currentColor;
}
.agent-dot::after {
  content: '';
  position: absolute;
  bottom: 0; left: 50%; transform: translateX(-50%);
  width: 20px; height: 18px;
  border-radius: 10px 10px 0 0;
  background: currentColor;
}
.agent-dot.active { color: var(--martz); }
.agent-dot.faded  { color: var(--gray-border); }

/* ── COMPARATIVO SIMPLES ── */
.compare-box {
  display: grid;
  grid-template-columns: 1fr auto 1fr;
  gap: 1.5rem;
  align-items: center;
  background: var(--white);
  border: 1px solid var(--gray-border);
  border-radius: 16px;
  padding: 2rem;
  margin-bottom: 2rem;
}
.compare-side {
  background: var(--gray-light);
  border-radius: 12px;
  padding: 1.5rem;
  text-align: center;
}
.compare-side.after {
  background: var(--martz-light);
  border: 1.5px solid var(--martz-mid);
}
.compare-label {
  font-size: 0.72rem;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.07em;
  color: var(--gray);
  margin-bottom: 1rem;
}
.compare-side.after .compare-label { color: var(--martz); }

.compare-agents-vis {
  display: flex;
  justify-content: center;
  gap: 0.4rem;
  margin-bottom: 0.75rem;
  flex-wrap: wrap;
}
.compare-stat {
  font-size: 0.83rem;
  padding: 0.35rem 0;
  border-top: 1px solid rgba(0,0,0,0.06);
  margin-top: 0.35rem;
  color: var(--text);
}
.compare-stat strong { color: var(--martz); }
.compare-side:not(.after) .compare-stat strong { color: var(--text); }

.compare-arrow {
  font-size: 2rem;
  color: var(--martz);
  text-align: center;
}

/* ── ALINHAMENTO CONTEÚDO ── */
.methods-grid {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 1rem;
}
.method-card {
  background: var(--white);
  border: 1px solid var(--gray-border);
  border-radius: 12px;
  overflow: hidden;
}
.method-head {
  background: var(--martz);
  color: white;
  padding: 0.875rem 1rem;
  font-weight: 700;
  font-size: 0.9rem;
}
.method-body { padding: 1rem; }
.method-desc {
  font-size: 0.8rem;
  color: var(--gray);
  margin-bottom: 0.875rem;
  line-height: 1.5;
  font-weight: 500;
  text-align: center;
}
.rating-row {
  display: flex;
  justify-content: space-between;
  align-items: center;
  font-size: 0.75rem;
  padding: 0.25rem 0;
}
.rating-label { color: var(--gray); }
.stars { letter-spacing: 0.05em; font-size: 0.8rem; }
.method-divider {
  border: none;
  border-top: 1px solid var(--gray-border);
  margin: 0.75rem 0;
}
.vant { font-size: 0.78rem; color: var(--green); font-weight: 600; margin-bottom: 0.2rem; }
.risco { font-size: 0.78rem; color: var(--ch-orange); font-weight: 600; margin-bottom: 0.2rem; }
.method-text { font-size: 0.76rem; color: var(--gray); line-height: 1.45; }

/* ── INFO BOX ── */
.info-box {
  background: var(--green-light);
  border-left: 3px solid var(--green);
  border-radius: 0 10px 10px 0;
  padding: 1rem 1.25rem;
  margin-top: 1.5rem;
  font-size: 0.875rem;
  color: var(--text);
}
.info-box strong { color: var(--green); }

/* ── DEMO CTA ── */
.demo-cta {
  background: linear-gradient(135deg, var(--martz-dark) 0%, var(--martz) 100%);
  border-radius: 16px;
  padding: 2.5rem;
  text-align: center;
  color: white;
  margin-top: 2rem;
}
.demo-cta h3 {
  font-size: 1.4rem;
  font-weight: 700;
  margin-bottom: 0.5rem;
}
.demo-cta p { opacity: 0.85; font-size: 0.9rem; margin-bottom: 1.5rem; }
.demo-btn {
  display: inline-block;
  background: white;
  color: var(--martz);
  font-weight: 700;
  font-size: 0.9rem;
  padding: 0.875rem 2rem;
  border-radius: 50px;
  cursor: pointer;
  transition: all 0.2s;
  border: none;
  font-family: 'DM Sans', sans-serif;
}
.demo-btn:hover {
  background: var(--martz-light);
  transform: translateY(-1px);
}

/* ── DIVIDER ── */
.section-divider {
  border: none;
  border-top: 1px solid var(--gray-border);
  margin: 2rem 0;
}

/* Responsive */
@media (max-width: 768px) {
  .hero-grid { grid-template-columns: 1fr 1fr; }
  .tag-grid { grid-template-columns: 1fr 1fr; }
  .scenarios-grid { grid-template-columns: 1fr; }
  .methods-grid { grid-template-columns: 1fr 1fr; }
  .compare-box { grid-template-columns: 1fr; }
  .compare-arrow { transform: rotate(90deg); }
  .premissas-row { grid-template-columns: 1fr 1fr; }
  .bar-label { width: 160px; font-size: 0.72rem; }
}
</style>
</head>
<body>

<!-- HEADER -->
<header class="site-header">
  <div class="logos">
    <!-- Martz logo SVG inline -->
    <svg width="80" height="28" viewBox="0 0 120 40" fill="none" xmlns="http://www.w3.org/2000/svg">
      <text x="0" y="32" font-family="DM Sans, sans-serif" font-weight="700" font-size="36" fill="#7B5EA7" letter-spacing="-2">martz</text>
    </svg>
    <div class="logo-sep"></div>
    <!-- Cloud Humans logo -->
    <div class="logo-ch">
      <svg width="52" height="20" viewBox="0 0 120 44" fill="none" xmlns="http://www.w3.org/2000/svg">
        <ellipse cx="22" cy="28" rx="18" ry="14" fill="#FFDCC8"/>
        <ellipse cx="38" cy="22" rx="16" ry="13" fill="#FFB899"/>
        <circle cx="54" cy="24" r="14" fill="#FF8C42"/>
      </svg>
      <span style="font-size:0.82rem;font-weight:600;color:#555">cloud humans</span>
    </div>
  </div>
  <div class="header-badge">Diagnóstico de Automação · Jan–Fev 2026</div>
</header>

<!-- NAV -->
<nav class="nav-tabs">
  <button class="nav-tab active" onclick="showTab('visao-geral', this)">Visão Geral</button>
  <button class="nav-tab" onclick="showTab('composicao', this)">Composição da Base</button>
  <button class="nav-tab" onclick="showTab('calculadora', this)">Calculadora de ROI</button>
  <button class="nav-tab" onclick="showTab('operacao', this)">Alinhamento Operacional</button>
</nav>

<main class="main">

  <!-- ══════════════════════════════════════════
       TAB 1: VISÃO GERAL
  ══════════════════════════════════════════ -->
  <div id="visao-geral" class="tab-content active">

    <div class="section-header">
      <div class="section-title">Diagnóstico de Automação — Martz</div>
      <div class="section-subtitle">Análise de Janeiro e Fevereiro de 2026 · 658 tickets de suporte</div>
      <div class="section-rule"></div>
    </div>

    <!-- Hero metrics -->
    <div class="hero-grid">
      <div class="hero-card featured">
        <div class="label">Teto de Automação</div>
        <div class="value">93%</div>
        <div class="sub">da base válida é automatizável</div>
        <div class="accent-bar"></div>
      </div>
      <div class="hero-card">
        <div class="label">Tickets Analisados</div>
        <div class="value">658</div>
        <div class="sub">Jan e Fev de 2026</div>
        <div class="accent-bar"></div>
      </div>
      <div class="hero-card">
        <div class="label">Conteúdo</div>
        <div class="value" style="color:var(--green)">86%</div>
        <div class="sub">567 tickets · teto 100%</div>
        <div class="accent-bar" style="background:var(--green)"></div>
      </div>
      <div class="hero-card">
        <div class="label">Edição</div>
        <div class="value" style="color:var(--ch-orange)">14%</div>
        <div class="sub">91 tickets · teto 50%</div>
        <div class="accent-bar" style="background:var(--ch-orange)"></div>
      </div>
    </div>

    <!-- Info sobre a amostra -->
    <div style="background:var(--martz-light);border-radius:12px;padding:1rem 1.5rem;margin-bottom:2rem;display:flex;align-items:center;gap:1rem;font-size:0.85rem;color:var(--martz-dark);">
      <span style="font-size:1.2rem">📋</span>
      <div>
        <strong>Sobre a análise:</strong> Amostra composta pelo volume de suporte de <strong>Janeiro e Fevereiro de 2026</strong> — <strong>658 tickets de suporte</strong> analisados.
      </div>
    </div>

    <!-- Como classificamos -->
    <div class="section-header">
      <div class="section-title" style="font-size:1.15rem">Como classificamos os tickets</div>
      <div class="section-rule"></div>
    </div>

    <div class="tag-grid" style="margin-bottom:2.5rem">
      <div class="tag-card green">
        <div class="tag-name">Conteúdo</div>
        <div class="tag-badge">TETO 100%</div>
        <div class="tag-desc">Dúvidas respondidas com base de conhecimento. A IA resolve sozinha, sem intervenção humana.</div>
      </div>
      <div class="tag-card orange">
        <div class="tag-name">Edição</div>
        <div class="tag-badge">TETO 50%</div>
        <div class="tag-desc">Requer ação em sistemas — altera dados no ERP ou plataforma. Depende de integração técnica.</div>
      </div>
      <div class="tag-card blue">
        <div class="tag-name">Visualização</div>
        <div class="tag-badge">TETO 70%</div>
        <div class="tag-desc">Consulta de status ou informação em sistemas, sem alterar dados.</div>
      </div>
      <div class="tag-card red">
        <div class="tag-name">Escalado N3</div>
        <div class="tag-badge" style="background:#FEF2F2;color:var(--red)">N/A</div>
        <div class="tag-desc">Requer decisão humana especializada. Encaminhamento para time técnico.</div>
      </div>
    </div>

    <div class="info-box">
      <strong>✓ Na Martz:</strong> 86% dos tickets válidos são de Conteúdo (teto 100%) e 14% são de Edição (teto 50%) — 
      base excelente para automação, com um teto ponderado de <strong>93%</strong>.
    </div>

  </div>

  <!-- ══════════════════════════════════════════
       TAB 2: COMPOSIÇÃO DA BASE
  ══════════════════════════════════════════ -->
  <div id="composicao" class="tab-content">

    <div class="section-header">
      <div class="section-title">Composição da Base de Suporte</div>
      <div class="section-subtitle">Top temas · base válida de 658 tickets (Jan–Fev 2026)</div>
      <div class="section-rule"></div>
    </div>

    <div class="comp-section">
      <div class="comp-title">Principais temas de suporte</div>
      <div class="comp-sub">Ordenado por volume · % sobre a base válida de 658 tickets</div>

      <div class="bar-row">
        <div class="bar-label">Multicanal / Envios (SMS, Email, WhatsApp)</div>
        <div class="bar-track"><div class="bar-fill" style="width:100%;background:var(--green)"><span>135 tickets</span></div></div>
        <div class="bar-pct">20%</div>
        <div class="bar-tag conteudo">Conteúdo</div>
      </div>
      <div class="bar-row">
        <div class="bar-label">Campanha Carrinho Abandonado</div>
        <div class="bar-track"><div class="bar-fill" style="width:79%;background:var(--green)"><span>107 tickets</span></div></div>
        <div class="bar-pct">16%</div>
        <div class="bar-tag conteudo">Conteúdo</div>
      </div>
      <div class="bar-row">
        <div class="bar-label">Integração Plataformas / Token / Webhook</div>
        <div class="bar-track"><div class="bar-fill" style="width:53%;background:var(--green)"><span>71 tickets</span></div></div>
        <div class="bar-pct">11%</div>
        <div class="bar-tag conteudo">Conteúdo</div>
      </div>
      <div class="bar-row">
        <div class="bar-label">Martz Academy – Acesso e Liberação</div>
        <div class="bar-track"><div class="bar-fill" style="width:33%;background:var(--green)"><span>44 tickets</span></div></div>
        <div class="bar-pct">7%</div>
        <div class="bar-tag conteudo">Conteúdo</div>
      </div>
      <div class="bar-row">
        <div class="bar-label">Cashback – Disparo, Prazos, Configuração</div>
        <div class="bar-track"><div class="bar-fill" style="width:31%;background:var(--green)"><span>42 tickets</span></div></div>
        <div class="bar-pct">6%</div>
        <div class="bar-tag conteudo">Conteúdo</div>
      </div>
      <div class="bar-row">
        <div class="bar-label">Faturamento / Insights / API</div>
        <div class="bar-track"><div class="bar-fill" style="width:26%;background:var(--ch-orange)"><span>35 tickets</span></div></div>
        <div class="bar-pct">5%</div>
        <div class="bar-tag edicao">Edição</div>
      </div>
      <div class="bar-row">
        <div class="bar-label">WhatsApp API – Configuração e Pagamento</div>
        <div class="bar-track"><div class="bar-fill" style="width:25%;background:var(--green)"><span>34 tickets</span></div></div>
        <div class="bar-pct">5%</div>
        <div class="bar-tag conteudo">Conteúdo</div>
      </div>
      <div class="bar-row">
        <div class="bar-label">Cancelamento de Conta / Retenção</div>
        <div class="bar-track"><div class="bar-fill" style="width:25%;background:var(--ch-orange)"><span>34 tickets</span></div></div>
        <div class="bar-pct">5%</div>
        <div class="bar-tag edicao">Edição</div>
      </div>
    </div>

    <div class="info-box" style="margin-top:1.5rem">
      <strong>✓ Concentração:</strong> Os 4 maiores temas representam 54% da base válida — 
      todos de Conteúdo, o que significa que um investimento inicial focado nesses temas já gera impacto imediato.
    </div>

  </div>

  <!-- ══════════════════════════════════════════
       TAB 3: CALCULADORA DE ROI
  ══════════════════════════════════════════ -->
  <div id="calculadora" class="tab-content">

    <div class="section-header">
      <div class="section-title">Calculadora de Impacto Financeiro</div>
      <div class="section-subtitle">Ajuste o número de agentes e veja o impacto em tempo real</div>
      <div class="section-rule"></div>
    </div>

    <div class="calc-wrapper">
      <div class="calc-header">
        <h3>Configure sua operação atual</h3>
        <p>As premissas de custo estão baseadas em benchmarks de mercado — ajuste o número de agentes conforme sua realidade</p>
      </div>
      <div class="calc-body">

        <!-- Input de agentes -->
        <div class="agents-input-row" style="gap:1rem;flex-wrap:wrap">
          <!-- Agentes -->
          <div class="agents-label" style="flex:1;min-width:160px">
            Agentes de suporte hoje
            <span>Custo total: <strong id="custoAtual">R$ 30.000/mês</strong></span>
          </div>
          <div style="display:flex;flex-direction:column;align-items:center;gap:0.25rem">
            <div style="font-size:0.7rem;font-weight:600;color:var(--martz);text-transform:uppercase;letter-spacing:0.05em">Agentes</div>
            <div class="agents-control">
              <button class="agent-btn" onclick="changeAgents(-1)">−</button>
              <div id="agentCount">6</div>
              <button class="agent-btn" onclick="changeAgents(+1)">+</button>
            </div>
          </div>
          <!-- Divisor -->
          <div style="width:1px;background:var(--martz-mid);align-self:stretch;opacity:0.4"></div>
          <!-- Tickets -->
          <div style="display:flex;flex-direction:column;align-items:center;gap:0.25rem">
            <div style="font-size:0.7rem;font-weight:600;color:var(--martz);text-transform:uppercase;letter-spacing:0.05em">Tickets/mês</div>
            <div class="agents-control">
              <button class="agent-btn" onclick="changeTickets(-50)">−</button>
              <div id="ticketCount" style="min-width:3.5rem;font-size:1.6rem">658</div>
              <button class="agent-btn" onclick="changeTickets(+50)">+</button>
            </div>
            <div style="font-size:0.68rem;color:var(--martz);opacity:0.7">±50 por clique</div>
          </div>
          <!-- Meta -->
          <div class="agents-meta" style="text-align:right;min-width:90px">
            Produtividade atual
            <strong id="prodAtual">110 tick/ag</strong>
          </div>
        </div>

        <!-- Premissas fixas -->
        <div style="font-size:0.78rem;color:var(--gray);margin-bottom:0.75rem;font-weight:600;text-transform:uppercase;letter-spacing:0.05em">Premissas fixas</div>
        <div class="premissas-row">
          <div class="prem-card">
            <div class="prem-val">R$ 5.000</div>
            <div class="prem-label">Custo/agente<br>full loaded</div>
          </div>
          <div class="prem-card">
            <div class="prem-val">R$ 3,89</div>
            <div class="prem-label">Custo IA<br>por ticket</div>
          </div>
          <div class="prem-card">
            <div class="prem-val">R$ 315</div>
            <div class="prem-label">Licença CloudChat<br>por agente/mês</div>
          </div>
          <div class="prem-card">
            <div class="prem-val">R$ 7.500</div>
            <div class="prem-label">Setup<br>one-time</div>
          </div>
        </div>

        <!-- Toggle sem/com CloudChat -->
        <div class="cc-toggle-row">
          <span style="font-size:0.8rem;color:var(--gray);font-weight:500">Exibir:</span>
          <button class="cc-btn" id="btnSem" onclick="setCC(false)">Sem CloudChat</button>
          <button class="cc-btn active" id="btnCom" onclick="setCC(true)">Com CloudChat</button>
        </div>

        <!-- Cenários -->
        <div class="scenarios-grid" id="scenariosGrid">
          <!-- preenchido pelo JS -->
        </div>

      </div>
    </div>

    <!-- Comparativo HOJE vs COM IA -->
    <div class="section-header" style="margin-top:0.5rem">
      <div class="section-title" style="font-size:1.15rem">Operação Atual vs. Com Cloud Humans</div>
      <div class="section-subtitle">Cenário Realista (65%) — atualiza conforme você muda o número de agentes</div>
      <div class="section-rule"></div>
    </div>

    <div class="compare-box" id="compareBox">
      <!-- preenchido pelo JS -->
    </div>

  </div>

  <!-- ══════════════════════════════════════════
       TAB 4: ALINHAMENTO OPERACIONAL
  ══════════════════════════════════════════ -->
  <div id="operacao" class="tab-content">

    <div class="section-header">
      <div class="section-title">Alinhamento da Operação — Criação de Conteúdo</div>
      <div class="section-subtitle">4 abordagens para criar o conteúdo que alimenta a IA · cada uma com trade-offs diferentes</div>
      <div class="section-rule"></div>
    </div>

    <div class="methods-grid">
      <div class="method-card">
        <div class="method-head">Extração de Tickets</div>
        <div class="method-body">
          <div class="method-desc">Agrupa padrões de P&R das interações reais a partir de IA Cloud Humans</div>
          <div class="rating-row"><span class="rating-label">Qualidade</span><span class="stars">⭐⭐⭐☆☆</span></div>
          <div class="rating-row"><span class="rating-label">Tempo</span><span class="stars">🕐🕐🕐☆☆</span></div>
          <div class="rating-row"><span class="rating-label">Esforço time</span><span class="stars">👤👤👤☆☆</span></div>
          <hr class="method-divider">
          <div class="vant">✓ Vantagem</div>
          <div class="method-text">Rápida construção e implementação</div>
          <div class="risco" style="margin-top:0.5rem">⚠ Risco</div>
          <div class="method-text">Trabalho de ajustes do time · Alucinação da IA</div>
        </div>
      </div>

      <div class="method-card">
        <div class="method-head">Central de Ajuda</div>
        <div class="method-body">
          <div class="method-desc">Realizar scraping da Central de Ajuda ou FAQ já existente e publicado</div>
          <div class="rating-row"><span class="rating-label">Qualidade</span><span class="stars">⭐⭐☆☆☆</span></div>
          <div class="rating-row"><span class="rating-label">Tempo</span><span class="stars">🕐☆☆☆☆</span></div>
          <div class="rating-row"><span class="rating-label">Esforço time</span><span class="stars">👤☆☆☆☆</span></div>
          <hr class="method-divider">
          <div class="vant">✓ Vantagem</div>
          <div class="method-text">Teste Playground em 1 semana · Atualização automática</div>
          <div class="risco" style="margin-top:0.5rem">⚠ Risco</div>
          <div class="method-text">Não captura como o cliente pergunta · Não engloba backoffice</div>
        </div>
      </div>

      <div class="method-card">
        <div class="method-head">PDFs / Manuais</div>
        <div class="method-body">
          <div class="method-desc">Transformar instruções para o usuário final em conteúdo para a IA</div>
          <div class="rating-row"><span class="rating-label">Qualidade</span><span class="stars">⭐⭐⭐⭐☆</span></div>
          <div class="rating-row"><span class="rating-label">Tempo</span><span class="stars">🕐🕐☆☆☆</span></div>
          <div class="rating-row"><span class="rating-label">Esforço time</span><span class="stars">👤👤☆☆☆</span></div>
          <hr class="method-divider">
          <div class="vant">✓ Vantagem</div>
          <div class="method-text">Utiliza grande volume de conteúdo já criado</div>
          <div class="risco" style="margin-top:0.5rem">⚠ Risco</div>
          <div class="method-text">Não captura como o cliente pergunta · Ajustes do time</div>
        </div>
      </div>

      <div class="method-card" style="border-color:var(--martz);box-shadow:0 4px 16px rgba(123,94,167,0.12)">
        <div class="method-head" style="background:var(--martz-dark)">Criar Manual ★</div>
        <div class="method-body">
          <div class="method-desc">Construir P&R a partir do conhecimento do time de atendimento</div>
          <div class="rating-row"><span class="rating-label">Qualidade</span><span class="stars">⭐⭐⭐⭐⭐</span></div>
          <div class="rating-row"><span class="rating-label">Tempo</span><span class="stars">🕐🕐🕐🕐☆</span></div>
          <div class="rating-row"><span class="rating-label">Esforço time</span><span class="stars">👤👤👤👤☆</span></div>
          <hr class="method-divider">
          <div class="vant">✓ Vantagem</div>
          <div class="method-text">Melhor controle sobre tom de voz e respostas</div>
          <div class="risco" style="margin-top:0.5rem">⚠ Risco</div>
          <div class="method-text">Time-to-value pode ser mais demorado</div>
        </div>
      </div>
    </div>

    <div class="info-box">
      <strong>✓ Recomendação:</strong> Combinar <strong>Extração de Tickets</strong> para quick wins imediatos
      com <strong>Criação Manual</strong> para garantir qualidade e controle do tom de voz a médio prazo.
    </div>

    <div class="demo-cta">
      <h3>Próximo passo: Demo ao vivo</h3>
      <p>Apresentamos a plataforma funcionando com casos reais do suporte Martz.<br>Você vê a IA em ação antes de qualquer decisão.</p>
      <button class="demo-btn">Agendar demonstração →</button>
    </div>

  </div>

</main>

<script>
// ── DATA ──────────────────────────────────────
const BASE_SAMPLE = 658;        // amostra analisada
const PCT_CONTEUDO = 567/658;   // 86.2%
const PCT_EDICAO   = 91/658;    // 13.8%
const TETO_CONTEUDO_val = 1.00;
const TETO_EDICAO_val   = 0.50;

let ticketsMonth = 658;  // editável pelo usuário

const CUSTO_IA      = 3.89;
const CUSTO_AGENTE  = 5000;
const LICENCA       = 315;
const SETUP         = 7500;

let agents    = 6;
let withCC    = true;  // com CloudChat default

// ── NAVIGATION ────────────────────────────────
function showTab(id, btn) {
  document.querySelectorAll('.tab-content').forEach(el => el.classList.remove('active'));
  document.querySelectorAll('.nav-tab').forEach(el => el.classList.remove('active'));
  document.getElementById(id).classList.add('active');
  btn.classList.add('active');
  if (id === 'calculadora') renderAll();
}

// ── CALCULADORA ───────────────────────────────
function changeAgents(delta) {
  agents = Math.max(1, Math.min(20, agents + delta));
  renderAll();
}

function changeTickets(delta) {
  ticketsMonth = Math.max(50, ticketsMonth + delta);
  document.getElementById('ticketCount').textContent = ticketsMonth;
  renderAll();
}

function setCC(val) {
  withCC = val;
  document.getElementById('btnSem').classList.toggle('active', !val);
  document.getElementById('btnCom').classList.toggle('active', val);
  renderAll();
}

function fmt(n) {
  return 'R$ ' + Math.round(n).toLocaleString('pt-BR');
}

function calcScenario(factor) {
  const vol_c    = Math.round(ticketsMonth * PCT_CONTEUDO);
  const vol_e    = ticketsMonth - vol_c;
  const ia_c     = Math.round(vol_c * TETO_CONTEUDO_val * factor);
  const ia_e     = Math.round(vol_e * TETO_EDICAO_val   * factor);
  const ia_total = ia_c + ia_e;
  const ia_pct   = (ia_total / ticketsMonth * 100).toFixed(0);
  const human    = ticketsMonth - ia_total;
  const prod     = Math.ceil(ticketsMonth / agents);
  const ag_needed= Math.max(1, Math.ceil(human / prod));
  const freed    = Math.max(0, agents - ag_needed);

  const cost_human = ag_needed * CUSTO_AGENTE;
  const cost_ia    = Math.round(ia_total * CUSTO_IA);
  const cost_lic   = ag_needed * LICENCA;

  const total_sem = cost_human + cost_ia;
  const total_com = cost_human + cost_ia + cost_lic;
  const total     = withCC ? total_com : total_sem;

  const current   = agents * CUSTO_AGENTE;
  const eco_mes   = current - total;
  const eco_ano   = eco_mes * 12 - SETUP;

  return { ia_total, ia_pct, ag_needed, freed, cost_ia,
           total_sem, total_com, total, eco_mes, eco_ano, current };
}

function renderAll() {
  const prod = Math.ceil(ticketsMonth / agents);
  document.getElementById('agentCount').textContent  = agents;
  document.getElementById('ticketCount').textContent = ticketsMonth;
  document.getElementById('custoAtual').textContent  = fmt(agents * CUSTO_AGENTE) + '/mês';
  document.getElementById('prodAtual').textContent   = prod + ' tick/ag';

  const sc = [
    { name: 'Conservador', factor: 0.50, featured: false },
    { name: 'Realista',    factor: 0.65, featured: true  },
    { name: 'Otimista',    factor: 0.85, featured: false },
  ];

  const grid = document.getElementById('scenariosGrid');
  grid.innerHTML = sc.map(s => {
    const d = calcScenario(s.factor);
    const ecoPos = d.eco_mes > 0;
    const yr1k = Math.round(d.eco_ano / 1000);
    const pctRed = d.freed > 0 ? Math.round(d.freed/agents*100) : 0;

    // Agent icons
    const icons = Array.from({length: Math.min(agents, 8)}, (_, i) =>
      `<div class="agent-dot ${i < d.ag_needed ? 'active' : 'faded'}"></div>`
    ).join('');

    return `
    <div class="scenario-card ${s.featured ? 'featured' : ''}">
      <div class="sc-head">
        ${s.name} &nbsp;<span style="opacity:0.7;font-size:0.78rem">${(s.factor*100).toFixed(0)}%</span>
        ${s.featured ? '<span class="sc-star">★ Realista</span>' : ''}
      </div>
      <div class="sc-body">
        <div style="display:flex;justify-content:center;gap:0.35rem;margin-bottom:0.5rem;flex-wrap:wrap">
          ${icons}
        </div>
        <div style="text-align:center;font-size:0.8rem;color:var(--gray);margin-bottom:0.875rem">
          <strong style="color:var(--martz)">${d.ag_needed} agente${d.ag_needed!==1?'s':''}</strong> necessários
          ${d.freed > 0 ? `· <span style="color:var(--green)">−${d.freed} libera${d.freed!==1?'dos':'do'}</span>` : ''}
        </div>
        <div class="sc-metric">
          <span class="sc-metric-label">IA resolve</span>
          <span class="sc-metric-val martz">${d.ia_total} tickets (${d.ia_pct}%)</span>
        </div>
        <div class="sc-metric">
          <span class="sc-metric-label">Custo IA/mês</span>
          <span class="sc-metric-val">${fmt(d.cost_ia)}</span>
        </div>
        <div class="sc-metric">
          <span class="sc-metric-label">Custo total</span>
          <span class="sc-metric-val">${fmt(d.total)}/mês</span>
        </div>
        <div class="sc-eco">
          <div class="sc-eco-label">Economia mensal</div>
          <div class="sc-eco-val" style="color:${ecoPos ? 'var(--green)' : 'var(--red)'}">
            ${ecoPos ? '' : '−'}${fmt(Math.abs(d.eco_mes))}
          </div>
          <div class="sc-eco-year">${yr1k > 0 ? 'R$ '+yr1k+'k' : '−R$ '+Math.abs(yr1k)+'k'} líquido no ano 1</div>
        </div>
      </div>
    </div>`;
  }).join('');

  // Comparativo (Realista)
  const r = calcScenario(0.65);
  const currentTotal = agents * CUSTO_AGENTE;

  const iconsHoje = Array.from({length: Math.min(agents,8)}, (_, i) =>
    `<div class="agent-dot active"></div>`).join('');
  const iconsDepois = Array.from({length: Math.min(agents,8)}, (_, i) =>
    `<div class="agent-dot ${i < r.ag_needed ? 'active' : 'faded'}"></div>`).join('');

  document.getElementById('compareBox').innerHTML = `
    <div class="compare-side">
      <div class="compare-label">Hoje</div>
      <div class="compare-agents-vis">${iconsHoje}</div>
      <div style="font-size:1.4rem;font-weight:700;color:var(--text);text-align:center;margin-bottom:0.5rem">${agents} agentes</div>
      <div class="compare-stat"><strong>${ticketsMonth} tickets/mês</strong> volume de suporte</div>
      <div class="compare-stat"><strong>${fmt(currentTotal)}/mês</strong> custo total</div>
    </div>
    <div class="compare-arrow">→</div>
    <div class="compare-side after">
      <div class="compare-label">Com Cloud Humans</div>
      <div class="compare-agents-vis">${iconsDepois}</div>
      <div style="font-size:1.4rem;font-weight:700;color:var(--martz);text-align:center;margin-bottom:0.5rem">${r.ag_needed} agente${r.ag_needed!==1?'s':''}</div>
      <div class="compare-stat"><strong>${r.ia_total} tickets/mês</strong> pela IA (${r.ia_pct}%)</div>
      <div class="compare-stat"><strong>${fmt(r.total)}/mês</strong> custo total</div>
      <div class="compare-stat" style="color:var(--green);font-weight:700;font-size:0.9rem">
        ${fmt(r.eco_mes)}/mês de economia
      </div>
    </div>`;
}

// Init calculadora silenciosamente
renderAll();
</script>
</body>
</html>
